---
title: "App Globals Report"
output: pdf_document
params:
  globals: NULL
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# App Globals Report

## Globals Overview

### Groesse
- Selection: `r params$globals$groesse$selection`
- Factor: `r params$globals$groesse$factor`
- Info Text: `r params$globals$groesse$info_text`
- Lower Bound (ug): `r params$globals$groesse$lo`
- Middle Value (oue): `r params$globals$groesse$mid`
- Upper Bound (og): `r params$globals$groesse$hi`

---

### Baujahr
- Selection: `r params$globals$baujahr$selection`
- Factor: `r params$globals$baujahr$factor`
- Info Text: `r params$globals$baujahr$info_text`

---

### Renovation
- Selection: `r paste(params$globals$renovation$selection, collapse = ", ")`
- Factor: `r params$globals$renovation$factor`
- Info Text: `r params$globals$renovation$info_text`

---

## Additional Notes

The above reflects the current state of the application’s globals.
